import { Routes } from '@angular/router';
import { TailorFormComponent } from './features/tailor/tailor-form.component';

export const routes: Routes = [
  { path: '', component: TailorFormComponent }
];
